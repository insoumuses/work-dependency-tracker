function applyGgetSnsTenkaiOverride_(parameters, botKun) {
  console.log('★★★★★ applyGgetSnsTenkaiOverride_ called ★★★★★');

  console.log('parameters exists: ' + !!parameters);
  console.log('botKun exists: ' + !!botKun);

  if (!parameters) {
    console.log('return reason: parameters is empty');
    return parameters;
  }

  console.log('parameters is array: ' + Array.isArray(parameters));
  console.log('parameters type: ' + typeof parameters);
  console.log('parameters keys: ' + Object.keys(parameters).join(', '));

  console.log('parameters preview:');
  console.log(JSON.stringify(parameters, null, 2).slice(0, 3000));

  return parameters;
}
